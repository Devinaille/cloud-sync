# cloud-sync service env (legacy / pure-env mode).
# Copy to .env and fill in real values; docker-compose reads these automatically.
#
# Prefer cloud-sync/config.example.yaml (mounted at /config/cloud-sync.yaml
# in the container) for new deploys — the YAML file overrides these values
# for any key it sets.

# OpenList
OPENLIST_URL=http://openlist:5244
OPENLIST_TOKEN=replace-me
# Storage ROOTS; cloud-sync appends a "media" subdir on upload.
OPENLIST_SRC_STORAGE=/local_media
OPENLIST_DST_STORAGE=/139yun_media
# false → skip_existing (keep cloud file if present); true → overwrite
OPENLIST_OVERWRITE=false

# Web UI
# Web UI listen address (default :8099; empty or "-" disables)
UI_LISTEN=:8099

# Watch paths (comma-separated absolute paths; fsnotify recurses each)
WATCH_DIRS=/mnt/basic/media/media,/mnt/basic/media/ani-rss

# Sync state
SYNC_STATUS_DIR=/mnt/basic/media/.sync_status

# Cleanup
CLEANUP_AFTER_HOURS=72
CLEANUP_DRY_RUN=true

# Upload pipeline
UPLOAD_CONCURRENCY=2
STABILIZE_WAIT_SECONDS=30
POLL_INTERVAL_SECONDS=3
TASK_TIMEOUT_SECONDS=1800

# Safety
ALLOWED_SOURCE_PREFIXES=/mnt/basic/media/media,/mnt/basic/media/ani-rss

# Logging
LOG_LEVEL=info
LOG_FILE=/var/log/cloud-sync/cloud-sync.log
